# fsossai.github.io

Federico Sossai's personal website. It is a dependency-free static site built with plain HTML, CSS, and JavaScript.

## Local preview

From the repository root, run:

```sh
python3 -m http.server 4000
```

Then open `http://localhost:4000`. There is no build step.
